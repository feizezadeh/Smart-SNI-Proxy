================================================
دستورات نهایی برای به‌روزرسانی سرور
دامنه: dns.dnsoverhttps.site
================================================

SSH به سرور و این دستورات را اجرا کنید:

ssh root@89.47.113.135

سپس:

cd /root/smartSNI && \
git fetch origin && \
git reset --hard origin/main && \
SERVER_IP=$(hostname -I | awk '{print $1}') && \
sed -i "s/<YOUR_IP>/$SERVER_IP/g" config.json && \
sed -i "s/<YOUR_HOST>/dns.dnsoverhttps.site/g" config.json && \
cat config.json && \
/usr/local/go/bin/go build -o smartsni main.go && \
systemctl restart sni.service && \
sleep 2 && \
systemctl status sni.service --no-pager && \
ss -tulnp | grep 8088

================================================
نتایج مورد انتظار:
================================================

✅ config.json با دامنه: dns.dnsoverhttps.site
✅ Build موفق
✅ Service: active (running)
✅ Port 8088: LISTEN

================================================
دسترسی به Web Panel:
================================================

🌐 http://89.47.113.135:8088/panel
👤 Username: admin
🔑 Password: admin

================================================
ویژگی‌های جدید:
================================================

1. اضافه کردن دامین بدون نیاز به IP
2. خودکار افزودن example.com + *.example.com
3. بخش User Management برای کنترل دسترسی
4. ایجاد لینک دعوت برای کاربران

================================================
فایل‌های اصلی پروژه (پاکسازی شده):
================================================

✅ main.go - کد اصلی
✅ webpanel.html - رابط کاربری
✅ config.json - پیکربندی
✅ install.sh - نصب
✅ README.md - راهنما
✅ nginx.conf - تنظیمات nginx
✅ go.mod/go.sum - وابستگی‌ها

همه فایل‌های اضافی حذف شدند!

================================================
